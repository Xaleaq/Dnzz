// let str = `Hello`;

// alert( str[0] );
// alert( str.at(0) );

// alert( str[str.length - 1] );
// alert( str.at(-1) );

// function showMood(){
//     const mood = prompt("Введите свое настроение:");
//     console.log(mood.toUpperCase());
//     console.log(mood.toLowerCase());
// }

// showMood();


function changeTextAndColor(){
    const setup = document.getElementById('setup');
    setup.textContent = 'Viscal Barca!';
    setup.style.color = 'blue'
}

// let str = "Строка";

// let newStr = str.slice(0, 3) + str.slice(4);
// console.log("Строка без 4-го символа:", newStr);

// let firstChar = newStr[0];
// let lastChar = newStr[newStr.length - 1];

// console.log("Первый символ:", firstChar);
// console.log("Последний символ:", lastChar);